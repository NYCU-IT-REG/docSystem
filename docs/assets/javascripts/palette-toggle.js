// Legacy custom palette toggle removed. Site uses Material theme's native palette.
// This file is intentionally left as a no-op to avoid injecting duplicate UI.
// Kept for compatibility with older deployments that reference this path.

/* no-op */
document.addEventListener('DOMContentLoaded', function () {
  // intentionally empty
});

